- 每个文件使用 `//go:build` + `// +build` 双行构建标签精确限定单一操作系统或兜底场景。
- 所有平台文件均导出同签名函数 `newClients() ([]wginternal.Client, error)`，由上层统一调用而不感知平台差异。
- 内核后端通过 `New()` 返回 `(client, ok, err)` 三元组，仅在 `ok==true` 时才将其加入客户端列表，体现可选能力探测模式。
- 用户态后端 `wguser.New()` 在所有平台中作为后备方案始终被追加到客户端列表末尾，确保降级可用性。